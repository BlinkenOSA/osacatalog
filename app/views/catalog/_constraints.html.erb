<% if query_has_constraints? %>
  <div id="appliedParams" class="clearfix constraints-container">

    <%=link_to t('blacklight.search.start_over'), start_over_path, :class => "btn btn-sm btn-danger start-again", :id=>"startOverLink" %>
    <%= render_constraints(params) %>
  </div>
<% end %>
